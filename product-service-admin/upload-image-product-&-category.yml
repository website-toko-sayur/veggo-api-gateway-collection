info:
  name: upload-image-product-&-category
  type: http
  seq: 3

http:
  method: POST
  url: "{{host-veggo-api-gateway}}/api/v1/admin/image-upload"
  body:
    type: multipart-form
    data:
      - name: image
        type: file
        value:
          - C:\Users\xylo\Documents\adit\random-image\daging_sapi_2.png
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
