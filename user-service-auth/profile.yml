info:
  name: profile
  type: http
  seq: 1

http:
  method: GET
  url: "{{host-veggo-api-gateway}}/api/v1/auth/profile"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
