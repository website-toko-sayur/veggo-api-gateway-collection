info:
  name: customer by id
  type: http
  seq: 2

http:
  method: GET
  url: "{{host-veggo-api-gateway}}/api/v1/admin/customers/:id"
  params:
    - name: id
      value: "5"
      type: path
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
