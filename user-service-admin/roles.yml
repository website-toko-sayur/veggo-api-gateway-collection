info:
  name: roles
  type: http
  seq: 3

http:
  method: GET
  url: "{{host-veggo-api-gateway}}/api/v1/admin/roles"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
