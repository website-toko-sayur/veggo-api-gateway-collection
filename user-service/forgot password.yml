info:
  name: forgot password
  type: http
  seq: 4

http:
  method: POST
  url: "{{host-veggo-api-gateway}}/api/v1/forgot-password"
  body:
    type: json
    data: |-
      {
        "email": "adit@mail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
